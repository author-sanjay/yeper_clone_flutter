import 'package:flutter/material.dart';
import 'package:yeper_user/Screens/Referal/Components/Body.dart';

class Referral extends StatelessWidget {
  const Referral({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}